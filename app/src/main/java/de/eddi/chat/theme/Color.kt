package de.eddi.chat.theme

import androidx.compose.ui.graphics.Color

// ── Primäre Markenfarben (aus login.css) ──────────────────────────────────
val Purple       = Color(0xFF8A2BE2)   // --primary
val PurpleLight  = Color(0xFFB066FF)   // --primary-light
val PurpleDark   = Color(0xFF5F27CD)   // Gradient-Ende

// ── Akzentfarbe (aus chat.css – .du border) ──────────────────────────────
val Green        = Color(0xFF00C040)
val GreenDark    = Color(0xFF00C030)

// ── Dunkel-Palette ────────────────────────────────────────────────────────
val DarkBg       = Color(0xFF080510)   // --bg-dark
val DarkSurface  = Color(0xFF14121F)   // ~card-bg solid
val DarkCard     = Color(0xFF1A0B2E)
val DarkBorder   = Color(0x338A2BE2)   // rgba(138,43,226,0.2)

// ── Hell-Palette ──────────────────────────────────────────────────────────
val LightBg      = Color(0xFFF5F0FF)
val LightSurface = Color(0xFFFFFFFF)
val LightCard    = Color(0xFFEDE7FF)

// ── Grau/Neutral ──────────────────────────────────────────────────────────
val TextGray     = Color(0xFFA0A0A0)   // --text-gray
val MsgGray      = Color(0xFF7C7A86)   // .fremd border & .time
val InputFill    = Color(0x0DFFFFFF)   // rgba(255,255,255,0.05)
